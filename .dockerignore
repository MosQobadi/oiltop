node_modules
.next
.git
.github

# env files — passed in at runtime, never baked into the image
.env
.env.*
!.env.example
!.env.test.example
!.env.production.example

# generated / local-only
lib/generated/prisma
*.tsbuildinfo
next-env.d.ts
tsconfig.tsbuildinfo

# tests, reports, docs
e2e
playwright-report
test-results
blob-report
playwright/.cache
graphify-out
*.md
!DEPLOYMENT.md

# misc
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
Dockerfile
docker-compose*.yml
.dockerignore

# host-nginx config, deployed to the VPS by hand — never needed in the image
deploy
